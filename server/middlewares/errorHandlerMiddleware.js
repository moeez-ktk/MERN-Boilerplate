/*
This file contains middleware functions to handle errors.
It could include functions to catch and handle errors that occur during
request processing, such as validation errors, database errors, or unexpected errors.
It may also format error responses to send back to the client.
*/

// Example of error handling middleware. modify it according to your requirements

// const handleErrors = (err, req, res, next) => {
//     console.error(err.stack);
//     res.status(500).json({ message: 'Internal server error' });
//   };

//   module.exports = {
//     handleErrors,
//   };
